@model IEnumerable<Chuyenphatnhanh.Models.UserConfigMstForm>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
 

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BRANCH_MST.REG_USER_NAME)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ROLE_MST.REG_USER_NAME)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.USER_MST.REG_USER_NAME)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>

                <td>
                    @Html.DisplayFor(modelItem => item.BRANCH_MST.REG_USER_NAME)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ROLE_MST.REG_USER_NAME)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.USER_MST.REG_USER_NAME)
                </td>
                <td>

                    @Html.ActionLink(Chuyenphatnhanh.Content.Texts.RGlobal.Edit, "Edit", new { id = item.USER_ID }) |
                    @Html.ActionLink(Chuyenphatnhanh.Content.Texts.RGlobal.View, "Details", new { id = item.USER_ID }) |
                    @Html.ActionLink(Chuyenphatnhanh.Content.Texts.RGlobal.Delete, "Delete", new { id = item.USER_ID })

                </td>
            </tr>
        }
    </tbody>

</table>
